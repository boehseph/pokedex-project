<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="/css/profile.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-left">
            <a href="/" id="findPokemonBtn" class="nav-button">Find Pokemon</a>
        </div>
        <div class="navbar-right">
            {{#if isLoggedIn}}
                {{#if isAdmin}}
                <div class="admin-dropdown">
                    <button class="nav-button admin-dropbtn">Users</button>
                    <div class="admin-dropdown-content">
                        <!-- Users will be loaded here via JavaScript -->
                    </div>
                </div>
                {{/if}}
                <a href="/user-pokedex" class="nav-button welcome-button">View Pokedex</a>
                <a href="#" id="logoutBtn" class="nav-button">Logout</a>
            {{else}}
                <a href="/login" id="loginBtn" class="nav-button">Login</a>
            {{/if}}
        </div>
    </nav>
    
    <div id="content">
        {{{body}}}
    </div>
    
    <script src="/js/app.js"></script>
    <script src="/js/navbar.js"></script>
</body>
</html>